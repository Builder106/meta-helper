# Pytest is no longer used for the backend (now Java + Spring Boot)
# Tests are run via ./gradlew test in the backend directory
# This file is kept for any Python helper scripts that may need tests
[pytest]
testpaths =
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v